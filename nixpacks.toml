# Nixpacks configuration pour Railway
# Fix: évite l'erreur mise/GitHub en utilisant directement Nix

[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "python3 snb_api_hf.py"
